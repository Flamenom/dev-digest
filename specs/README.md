# specs — DevDigest (cross-cutting)

Cross-package feature/contract specifications (course lesson L0x work that spans
more than one package). Add `NN-feature.md` here and link it from the root
`CLAUDE.md` under "Read when". Package-local specs live in `<package>/specs/`.
