# docs — @devdigest/e2e

Deep-dive documentation for the e2e suite (flow-authoring conventions, the
hermetic runner, CI wiring). Add topic files here and link them from
`../CLAUDE.md` → "Read when".

Note: `specs/` in this package is **flow specs** (`NN-name.flow.json`), not
lesson specs — see `../CLAUDE.md`.
