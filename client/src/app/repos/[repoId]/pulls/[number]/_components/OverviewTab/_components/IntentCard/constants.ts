/** Height of the loading skeleton shown while the intent query resolves. */
export const SKELETON_HEIGHT = 120;

/** Icon size for the check/x bullets in the scope columns. */
export const SCOPE_BULLET_ICON_SIZE = 13;
