/** Skeleton height while the blast payload loads (mirrors IntentCard). */
export const SKELETON_HEIGHT = 180;

/** file:line label cap before the caller-symbol suffix is dropped. */
export const CALLER_SYMBOL_MAX_CHARS = 28;
