# specs — @devdigest/api

Feature/contract specifications for server-side lesson work (L0x): new modules,
endpoints, and schema changes. Add `NN-feature.md` here and link it from
`../CLAUDE.md` → "Read when".
